
#include <stdio.h>
#include<string.h>
int main()
{
    char str[101];
    int l=0,i;
    gets(str);
    l=strlen(str)-1;
    for(i=0;i<=l;i++)
    {
      if(str[i]>='A' && str[i]<='Z')
      str[i]+=32;
      else
      str[i]-=32;
    }
    printf("%s",str);
}
